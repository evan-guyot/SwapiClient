import { IDictionaryContent } from "@/interfaces/main";

export default function SwapiVehicles(props: { data: IDictionaryContent }) {
  const { data } = props;

  return "Swapi Vehicles Composant";
}
