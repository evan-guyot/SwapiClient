import { IDictionaryContent } from "@/interfaces/main";

export default function SwapiStarships(props: { data: IDictionaryContent }) {
  const { data } = props;

  return "Swapi Starships Composant";
}
